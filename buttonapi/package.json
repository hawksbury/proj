{
  "name": "buttonapi",
  "version": "1.0.0",
  "description": "Alert simulator — browser button and Pico W trigger for the disaster response system",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
